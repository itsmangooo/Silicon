# Services

Silicon is a modular monolith. No independent runtime service is required for Milestone 1.

This directory is reserved for processes that genuinely need a separate lifecycle. The planned typed-operation Silicon Agent may eventually live under `services/agent/`; it is intentionally not implemented and no unrestricted remote shell protocol exists.
