import { api, organizationPath } from '../lib/api.js'
import { useWorkspace } from '../state/WorkspaceContext.jsx'
import { EmptyState, ErrorNotice, LoadingRows, Mono, Page, Section, formatDate, useResource } from '../components/ui.jsx'

export function AuditPage(){const {organizationId}=useWorkspace();const resource=useResource(()=>organizationId?api(organizationPath(organizationId,'/audit-events')):Promise.resolve({auditEvents:[]}),[organizationId]);return <Page title="Audit" description="Security-relevant organization activity with request correlation."><ErrorNotice error={resource.error}/><Section title="Audit events" description="Metadata is intentionally limited and must never contain secrets."><table><thead><tr><th>Time</th><th>Actor</th><th>Action</th><th>Resource</th><th>Request ID</th></tr></thead><tbody>{resource.loading?<LoadingRows columns={5}/>:resource.data?.auditEvents.length?resource.data.auditEvents.map(item=><tr key={item.id}><td data-label="Time">{formatDate(item.createdAt)}</td><td data-label="Actor">{item.actorName||'System'}</td><td data-label="Action"><Mono>{item.action}</Mono></td><td data-label="Resource">{item.resourceType}{item.resourceId&&<><br/><Mono>{item.resourceId.slice(0,8)}…</Mono></>}</td><td data-label="Request ID"><Mono>{item.requestId.slice(0,8)}…</Mono></td></tr>):<tr><td colSpan="5"><EmptyState title="No audit events">Security-relevant actions will appear here.</EmptyState></td></tr>}</tbody></table></Section></Page>}
