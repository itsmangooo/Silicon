import { useState } from 'react'
import { api, organizationPath } from '../lib/api.js'
import { useWorkspace } from '../state/WorkspaceContext.jsx'
import { CreateButton, Dialog, EmptyState, ErrorNotice, Field, LoadingRows, Mono, Notice, Page, Section, Status, SubmitRow, formatDate, useResource } from '../components/ui.jsx'

export function DeploymentsPage(){const {organizationId}=useWorkspace();const [open,setOpen]=useState(false);const resource=useResource(async()=>{if(!organizationId)return {deployments:[],applications:[]};const [deployments,applications]=await Promise.all([api(organizationPath(organizationId,'/deployments')),api(organizationPath(organizationId,'/applications'))]);return {deployments:deployments.deployments,applications:applications.applications}},[organizationId]);const names=Object.fromEntries((resource.data?.applications||[]).map(item=>[item.id,item.name]));return <Page title="Deployments" description="Immutable deployment history with explicit state transitions." actions={organizationId&&<CreateButton onClick={()=>setOpen(true)}>Create deployment record</CreateButton>}><Notice>Runtime execution is intentionally unavailable. Creating a record leaves it in <Mono>queued</Mono> until a future job/runtime adapter processes it.</Notice><ErrorNotice error={resource.error}/><Section title="Deployment history"><table><thead><tr><th>ID</th><th>Application</th><th>Status</th><th>Source</th><th>Revision</th><th>Created</th></tr></thead><tbody>{resource.loading?<LoadingRows columns={6}/>:resource.data.deployments.length?resource.data.deployments.map(item=><tr key={item.id}><td data-label="ID"><Mono>#{item.number}</Mono></td><td data-label="Application">{names[item.applicationId]||'Unknown'}</td><td data-label="Status"><Status value={item.status}/></td><td data-label="Source">{item.source||'—'}</td><td data-label="Revision"><Mono>{item.sourceRevision||'—'}</Mono></td><td data-label="Created">{formatDate(item.createdAt)}</td></tr>):<tr><td colSpan="6"><EmptyState title="No deployments yet">Create a deployment record after registering an application. No workload will be executed.</EmptyState></td></tr>}</tbody></table>{resource.data&&<DeploymentForm open={open} onClose={()=>setOpen(false)} applications={resource.data.applications} onSaved={resource.refresh}/>}</Section></Page>}

function DeploymentForm({open,onClose,applications,onSaved}){const {organizationId}=useWorkspace();const [error,setError]=useState('');const [submitting,setSubmitting]=useState(false);const submit=async(event)=>{event.preventDefault();setSubmitting(true);setError('');const values=Object.fromEntries(new FormData(event.currentTarget));const applicationId=values.applicationId;delete values.applicationId;try{await api(organizationPath(organizationId,`/applications/${applicationId}/deployments`),{method:'POST',body:values});await onSaved();onClose()}catch(requestError){setError(requestError.message)}finally{setSubmitting(false)}};return <Dialog title="Create deployment record" open={open} onClose={onClose}>{applications.length?<form className="form-stack" onSubmit={submit}><Field label="Application"><select name="applicationId" required>{applications.map(item=><option key={item.id} value={item.id}>{item.name}</option>)}</select></Field><Field label="Source"><input name="source" placeholder="registry or repository reference"/></Field><Field label="Source revision"><input name="sourceRevision" className="mono" placeholder="commit or digest"/></Field><Field label="Image"><input name="image" className="mono"/></Field><ErrorNotice error={error}/><SubmitRow submitting={submitting} onCancel={onClose} label="Create record"/></form>:<EmptyState title="An application is required">Register an application before creating a deployment record.</EmptyState>}</Dialog>}
